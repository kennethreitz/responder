name: "Tests"

on:
  push:
    branches: [ main ]
  pull_request: ~
  workflow_dispatch:

# Cancel redundant in-progress jobs.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  test:
    name: "Python ${{ matrix.python-version }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [
          "3.11",
          "3.12",
          "3.13",
          "3.14",
          "3.14t",
          "3.15",
          "pypy3.11",
        ]

    steps:
    - uses: actions/checkout@v7

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        activate-environment: true
        enable-cache: true
        cache-suffix: ${{ matrix.python-version }}
        python-version: ${{ matrix.python-version }}

    - name: Install package
      # Editable install so the cache never serves a stale pre-built wheel
      # (per-version cache once shipped an old responder, masking errors.py fixes).
      run: uv pip install -e '.[develop,test]'

    - name: Run tests
      run: pytest

  client-codegen:
    name: "Generated client syntax checks"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        activate-environment: true
        enable-cache: true
        python-version: "3.12"

    - name: Install package
      run: uv pip install -e '.[develop,test]'

    # Toolchains for the generated clients, so JS/TS/Ruby/PHP are
    # compile-verified rather than only string-asserted.
    - uses: actions/setup-node@v4
      with:
        node-version: "20"

    - uses: denoland/setup-deno@v2
      with:
        deno-version: v2.x

    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: "3.3"

    - uses: shivammathur/setup-php@v2
      with:
        php-version: "8.3"

    - name: Verify generated clients in every language
      run: pytest tests/test_clientgen.py -q -rs

  release-guard:
    name: "Release guard"
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        activate-environment: true
        enable-cache: true
        python-version: "3.12"

    - name: Verify release metadata and artifacts
      run: >
        uv run --extra release --extra test
        python scripts/release_check.py
        --skip-tests --skip-lint --skip-types --skip-docs
